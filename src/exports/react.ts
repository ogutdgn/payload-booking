// Booking UI for the host site ('use client'). Browser-safe: no node:crypto, no secrets,
// and never imports @payloadcms/ui.
// TODO(ring 5): AvailabilityPicker, BookingForm, CancelView, defaultMessages.
export {}
